-- Add migration script here
alter table game_player_stats
    add column if not exists player_name text,
    add column if not exists allowed_stolen_bases smallint generated always as (coalesce((data->'allowed_stolen_bases')::smallint, 0)) stored,
    add column if not exists allowed_stolen_bases_risp smallint generated always as (coalesce((data->'allowed_stolen_bases_risp')::smallint, 0)) stored,
    add column if not exists appearances smallint generated always as (coalesce((data->'appearances')::smallint, 0)) stored,
    add column if not exists assists smallint generated always as (coalesce((data->'assists')::smallint, 0)) stored,
    add column if not exists assists_risp smallint generated always as (coalesce((data->'assists_risp')::smallint, 0)) stored,
    add column if not exists at_bats smallint generated always as (coalesce((data->'at_bats')::smallint, 0)) stored,
    add column if not exists at_bats_risp smallint generated always as (coalesce((data->'at_bats_risp')::smallint, 0)) stored,
    add column if not exists balks smallint generated always as (coalesce((data->'balks')::smallint, 0)) stored,
    add column if not exists balks_risp smallint generated always as (coalesce((data->'balks_risp')::smallint, 0)) stored,
    add column if not exists batters_faced smallint generated always as (coalesce((data->'batters_faced')::smallint, 0)) stored,
    add column if not exists batters_faced_risp smallint generated always as (coalesce((data->'batters_faced_risp')::smallint, 0)) stored,
    add column if not exists blown_saves smallint generated always as (coalesce((data->'blown_saves')::smallint, 0)) stored,
    add column if not exists caught_double_play smallint generated always as (coalesce((data->'caught_double_play')::smallint, 0)) stored,
    add column if not exists caught_double_play_risp smallint generated always as (coalesce((data->'caught_double_play_risp')::smallint, 0)) stored,
    add column if not exists caught_stealing smallint generated always as (coalesce((data->'caught_stealing')::smallint, 0)) stored,
    add column if not exists caught_stealing_risp smallint generated always as (coalesce((data->'caught_stealing_risp')::smallint, 0)) stored,
    add column if not exists complete_games smallint generated always as (coalesce((data->'complete_games')::smallint, 0)) stored,
    add column if not exists double_plays smallint generated always as (coalesce((data->'double_plays')::smallint, 0)) stored,
    add column if not exists double_plays_risp smallint generated always as (coalesce((data->'double_plays_risp')::smallint, 0)) stored,
    add column if not exists doubles smallint generated always as (coalesce((data->'doubles')::smallint, 0)) stored,
    add column if not exists doubles_risp smallint generated always as (coalesce((data->'doubles_risp')::smallint, 0)) stored,
    add column if not exists earned_runs smallint generated always as (coalesce((data->'earned_runs')::smallint, 0)) stored,
    add column if not exists earned_runs_risp smallint generated always as (coalesce((data->'earned_runs_risp')::smallint, 0)) stored,
    add column if not exists errors smallint generated always as (coalesce((data->'errors')::smallint, 0)) stored,
    add column if not exists errors_risp smallint generated always as (coalesce((data->'errors_risp')::smallint, 0)) stored,
    add column if not exists field_out smallint generated always as (coalesce((data->'field_out')::smallint, 0)) stored,
    add column if not exists field_out_risp smallint generated always as (coalesce((data->'field_out_risp')::smallint, 0)) stored,
    add column if not exists fielders_choice smallint generated always as (coalesce((data->'fielders_choice')::smallint, 0)) stored,
    add column if not exists fielders_choice_risp smallint generated always as (coalesce((data->'fielders_choice_risp')::smallint, 0)) stored,
    add column if not exists flyouts smallint generated always as (coalesce((data->'flyouts')::smallint, 0)) stored,
    add column if not exists flyouts_risp smallint generated always as (coalesce((data->'flyouts_risp')::smallint, 0)) stored,
    add column if not exists force_outs smallint generated always as (coalesce((data->'force_outs')::smallint, 0)) stored,
    add column if not exists force_outs_risp smallint generated always as (coalesce((data->'force_outs_risp')::smallint, 0)) stored,
    add column if not exists games_finished smallint generated always as (coalesce((data->'games_finished')::smallint, 0)) stored,
    add column if not exists grounded_into_double_play smallint generated always as (coalesce((data->'grounded_into_double_play')::smallint, 0)) stored,
    add column if not exists grounded_into_double_play_risp smallint generated always as (coalesce((data->'grounded_into_double_play_risp')::smallint, 0)) stored,
    add column if not exists groundout smallint generated always as (coalesce((data->'groundout')::smallint, 0)) stored,
    add column if not exists groundout_risp smallint generated always as (coalesce((data->'groundout_risp')::smallint, 0)) stored,
    add column if not exists groundouts smallint generated always as (coalesce((data->'groundouts')::smallint, 0)) stored,
    add column if not exists groundouts_risp smallint generated always as (coalesce((data->'groundouts_risp')::smallint, 0)) stored,
    add column if not exists hit_batters smallint generated always as (coalesce((data->'hit_batters')::smallint, 0)) stored,
    add column if not exists hit_batters_risp smallint generated always as (coalesce((data->'hit_batters_risp')::smallint, 0)) stored,
    add column if not exists hit_by_pitch smallint generated always as (coalesce((data->'hit_by_pitch')::smallint, 0)) stored,
    add column if not exists hit_by_pitch_risp smallint generated always as (coalesce((data->'hit_by_pitch_risp')::smallint, 0)) stored,
    add column if not exists hits_allowed smallint generated always as (coalesce((data->'hits_allowed')::smallint, 0)) stored,
    add column if not exists hits_allowed_risp smallint generated always as (coalesce((data->'hits_allowed_risp')::smallint, 0)) stored,
    add column if not exists home_run_challenge_appearances smallint generated always as (coalesce((data->'home_run_challenge_appearances')::smallint, 0)) stored,
    add column if not exists home_run_challenge_home_runs smallint generated always as (coalesce((data->'home_run_challenge_home_runs')::smallint, 0)) stored,
    add column if not exists home_run_challenge_home_runs_allowed smallint generated always as (coalesce((data->'home_run_challenge_home_runs_allowed')::smallint, 0)) stored,
    add column if not exists home_runs smallint generated always as (coalesce((data->'home_runs')::smallint, 0)) stored,
    add column if not exists home_runs_allowed smallint generated always as (coalesce((data->'home_runs_allowed')::smallint, 0)) stored,
    add column if not exists home_runs_allowed_risp smallint generated always as (coalesce((data->'home_runs_allowed_risp')::smallint, 0)) stored,
    add column if not exists home_runs_risp smallint generated always as (coalesce((data->'home_runs_risp')::smallint, 0)) stored,
    add column if not exists inherited_runners smallint generated always as (coalesce((data->'inherited_runners')::smallint, 0)) stored,
    add column if not exists inherited_runners_risp smallint generated always as (coalesce((data->'inherited_runners_risp')::smallint, 0)) stored,
    add column if not exists inherited_runs_allowed smallint generated always as (coalesce((data->'inherited_runs_allowed')::smallint, 0)) stored,
    add column if not exists inherited_runs_allowed_risp smallint generated always as (coalesce((data->'inherited_runs_allowed_risp')::smallint, 0)) stored,
    add column if not exists left_on_base smallint generated always as (coalesce((data->'left_on_base')::smallint, 0)) stored,
    add column if not exists left_on_base_risp smallint generated always as (coalesce((data->'left_on_base_risp')::smallint, 0)) stored,
    add column if not exists lineouts smallint generated always as (coalesce((data->'lineouts')::smallint, 0)) stored,
    add column if not exists lineouts_risp smallint generated always as (coalesce((data->'lineouts_risp')::smallint, 0)) stored,
    add column if not exists losses smallint generated always as (coalesce((data->'losses')::smallint, 0)) stored,
    add column if not exists mound_visits smallint generated always as (coalesce((data->'mound_visits')::smallint, 0)) stored,
    add column if not exists no_hitters smallint generated always as (coalesce((data->'no_hitters')::smallint, 0)) stored,
    add column if not exists outs smallint generated always as (coalesce((data->'outs')::smallint, 0)) stored,
    add column if not exists perfect_games smallint generated always as (coalesce((data->'perfect_games')::smallint, 0)) stored,
    add column if not exists pitches_thrown smallint generated always as (coalesce((data->'pitches_thrown')::smallint, 0)) stored,
    add column if not exists pitches_thrown_risp smallint generated always as (coalesce((data->'pitches_thrown_risp')::smallint, 0)) stored,
    add column if not exists plate_appearances smallint generated always as (coalesce((data->'plate_appearances')::smallint, 0)) stored,
    add column if not exists plate_appearances_risp smallint generated always as (coalesce((data->'plate_appearances_risp')::smallint, 0)) stored,
    add column if not exists popouts smallint generated always as (coalesce((data->'popouts')::smallint, 0)) stored,
    add column if not exists popouts_risp smallint generated always as (coalesce((data->'popouts_risp')::smallint, 0)) stored,
    add column if not exists putouts smallint generated always as (coalesce((data->'putouts')::smallint, 0)) stored,
    add column if not exists putouts_risp smallint generated always as (coalesce((data->'putouts_risp')::smallint, 0)) stored,
    add column if not exists quality_starts smallint generated always as (coalesce((data->'quality_starts')::smallint, 0)) stored,
    add column if not exists reached_on_error smallint generated always as (coalesce((data->'reached_on_error')::smallint, 0)) stored,
    add column if not exists reached_on_error_risp smallint generated always as (coalesce((data->'reached_on_error_risp')::smallint, 0)) stored,
    add column if not exists runners_caught_stealing smallint generated always as (coalesce((data->'runners_caught_stealing')::smallint, 0)) stored,
    add column if not exists runners_caught_stealing_risp smallint generated always as (coalesce((data->'runners_caught_stealing_risp')::smallint, 0)) stored,
    add column if not exists runs smallint generated always as (coalesce((data->'runs')::smallint, 0)) stored,
    add column if not exists runs_batted_in smallint generated always as (coalesce((data->'runs_batted_in')::smallint, 0)) stored,
    add column if not exists runs_batted_in_risp smallint generated always as (coalesce((data->'runs_batted_in_risp')::smallint, 0)) stored,
    add column if not exists runs_risp smallint generated always as (coalesce((data->'runs_risp')::smallint, 0)) stored,
    add column if not exists sac_flies smallint generated always as (coalesce((data->'sac_flies')::smallint, 0)) stored,
    add column if not exists sac_flies_risp smallint generated always as (coalesce((data->'sac_flies_risp')::smallint, 0)) stored,
    add column if not exists sacrifice_double_plays smallint generated always as (coalesce((data->'sacrifice_double_plays')::smallint, 0)) stored,
    add column if not exists sacrifice_double_plays_risp smallint generated always as (coalesce((data->'sacrifice_double_plays_risp')::smallint, 0)) stored,
    add column if not exists saves smallint generated always as (coalesce((data->'saves')::smallint, 0)) stored,
    add column if not exists shutouts smallint generated always as (coalesce((data->'shutouts')::smallint, 0)) stored,
    add column if not exists singles smallint generated always as (coalesce((data->'singles')::smallint, 0)) stored,
    add column if not exists singles_risp smallint generated always as (coalesce((data->'singles_risp')::smallint, 0)) stored,
    add column if not exists starts smallint generated always as (coalesce((data->'starts')::smallint, 0)) stored,
    add column if not exists stolen_bases smallint generated always as (coalesce((data->'stolen_bases')::smallint, 0)) stored,
    add column if not exists stolen_bases_risp smallint generated always as (coalesce((data->'stolen_bases_risp')::smallint, 0)) stored,
    add column if not exists strikeouts smallint generated always as (coalesce((data->'strikeouts')::smallint, 0)) stored,
    add column if not exists strikeouts_risp smallint generated always as (coalesce((data->'strikeouts_risp')::smallint, 0)) stored,
    add column if not exists struck_out smallint generated always as (coalesce((data->'struck_out')::smallint, 0)) stored,
    add column if not exists struck_out_risp smallint generated always as (coalesce((data->'struck_out_risp')::smallint, 0)) stored,
    add column if not exists triples smallint generated always as (coalesce((data->'triples')::smallint, 0)) stored,
    add column if not exists triples_risp smallint generated always as (coalesce((data->'triples_risp')::smallint, 0)) stored,
    add column if not exists unearned_runs smallint generated always as (coalesce((data->'unearned_runs')::smallint, 0)) stored,
    add column if not exists unearned_runs_risp smallint generated always as (coalesce((data->'unearned_runs_risp')::smallint, 0)) stored,
    add column if not exists walked smallint generated always as (coalesce((data->'walked')::smallint, 0)) stored,
    add column if not exists walked_risp smallint generated always as (coalesce((data->'walked_risp')::smallint, 0)) stored,
    add column if not exists walks smallint generated always as (coalesce((data->'walks')::smallint, 0)) stored,
    add column if not exists walks_risp smallint generated always as (coalesce((data->'walks_risp')::smallint, 0)) stored,
    add column if not exists wins smallint generated always as (coalesce((data->'wins')::smallint, 0)) stored;
